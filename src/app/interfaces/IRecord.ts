export default interface Record {
  id: string;
  recordNumber: string;
  firstName: string;
  middleName?: string;
  lastName: string;
  address: string;
  phoneNumber: string;
  dateOfBirth: string;
  lastVisit: string;
}
